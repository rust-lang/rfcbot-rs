{{outlet}}

{{high-charts mode=model.pr.open_per_day.mode
              content=model.pr.open_per_day.data
              chartOptions=model.pr.open_per_day.opts
              them=model.pr.open_per_day.theme }}
