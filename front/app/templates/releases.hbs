{{outlet}}

<h3>Stable Releases</h3>

<h4>Rust 1.{{model.stable.next_version}} will release on {{model.stable.next_date}}.</h4>

<h5>Rust 1.{{model.stable.previous_version}} was released on {{model.stable.previous_date}}.</h5>


<h3>Nightly Releases</h3>

<div class="row">
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Released?</th>
        <th>Failed Builds</th>
      </tr>
    </thead>
    <tbody>
      {{#each model.nightlies as |r|}}
        <tr>
          <td>{{r.nightly.date}}</td>
          {{#if r.nightly.released}}

            <td style="background: #DFF0D8">
              &#x2713; Yes
            </td>
            <td></td>

          {{else}}

            <td style="background: #F2DEDE">
              <div><strong>&#x2717; No</strong></div>
            </td>
            <td>
              <ul>
                {{#each r.builds as |b|}}
                  <li>
                      <a href="http://buildbot.rust-lang.org/builders/{{b.builder_name}}/builds/{{b.number}}" target="_blank">
                          {{b.builder_name}}
                      </a>
                  </li>

          {{else}}
            No failed builds found. Perhaps this nightly hasn't run yet?
                {{/each}}
              </ul>
            </td>
          {{/if}}

        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
